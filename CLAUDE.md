# このリポジトリで守ること

田尻慎太郎の個人サイト。公開物なので、書いた内容はそのまま外に出る。

## 内容

- **事実だけを書く。** 肩書き・所属・年は本人確認済みの情報に限る。推測で補わない
- **数値を書くときは出典と時点を併記する。** 出典が出せない数値は載せない
- **AI生成画像を使わない。** 写真は本人が用意したものだけ
- 業績・発表の一覧は researchmap を正とし、このサイトには要約とリンクだけを置く
- 学生・共著者・学内の人の名前を、本人の許可なく載せない

## 作り

- 公開されるのは `public/` だけ。`docs/` は非公開のメモ（Pages の出力先が `public` なので配信されない）
- ビルドしない。HTML 1枚 + CSS はインライン。フレームワーク・npm を導入しない
- 外部スクリプトは GA4（gtag）のみ。他のタグ・トラッカーを増やさない
- スマホ幅（〜400px）で崩れないこと。ダークモードに追随すること

## 変更の出し方

- `main` に直接 push しない。必ず branch → PR
- PR の説明に「何を・なぜ」を1〜2行。Cloudflare のプレビューURLで確認してから Merge
- 計測イベントを足す・変えるときは `docs/experiments/` の該当ファイルも同時に直す
